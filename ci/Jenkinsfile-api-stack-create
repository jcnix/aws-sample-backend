pipeline {
    agent {
        label 'build'
    }
    stages {
        stage('create s3 bucket') {
            steps {
                sh '''
                    aws cloudformation deploy --template-file infrastructure/cloudformation/api.yml --stack-name rest-api
                '''
            }
        }
    }
}