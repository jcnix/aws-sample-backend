pipeline {
    agent {
        label 'build'
    }
    stages {
        stage('create lambda stack') {
            steps {
                sh '''
                    aws cloudformation deploy --template-file infrastructure/cloudformation/lambda.yml --stack-name api-lambda --capabilities CAPABILITY_IAM
                '''
            }
        }
    }
}